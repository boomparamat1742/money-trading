# Runtime dependencies (ที่โค้ดใช้จริง — ตรวจแล้วด้วยการสแกน import)
#
# หมายเหตุ: quant core + backtest + Edge Lab ใช้ Python standard library ล้วน
# รายการข้างล่างจำเป็นเฉพาะ live layer และการต่อฐานข้อมูล

websockets>=12.0          # Binance WebSocket stream
aiohttp>=3.9              # REST gap-recovery + LINE/Telegram push
anthropic>=0.40           # Claude AI context (slow path, ปิดได้)
psycopg[binary]>=3.2      # Supabase/PostgreSQL (ใช้เมื่อตั้ง DATABASE_URL)
python-dotenv>=1.0        # โหลด .env
