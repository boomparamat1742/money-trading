# secrets — never commit
# .env.* ครอบไฟล์สำรอง/ไฟล์ชั่วคราวที่มีความลับด้วย (.env.bak ฯลฯ)
.env
.env.*
!.env.example

# fetched market data (regenerate via fetch scripts)
data/

# python
__pycache__/
*.pyc
.pytest_cache/

# editor / tool config
.claude/

# ตัวแปรสำหรับวางใน Railway (มีความลับ)
railway-vars.txt
